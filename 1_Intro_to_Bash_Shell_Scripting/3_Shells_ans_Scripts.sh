#shell is an interface between the user and kernel, it gets starts when the user logs in and starts the terminal
#echo $0 OR echo $SHELL = type comamnd in terminal to see that what shell has started
#to get list of all valid login shells by dispalying the content of /etc/shells = cat /etc/shells 
#to change the user default shell = chsh -s /bin/bash

#Shell script is an executable text file that contains shell commands and other specific programming structures